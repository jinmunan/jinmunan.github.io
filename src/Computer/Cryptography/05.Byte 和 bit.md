---
icon: page
order: 5
---
# Byte 和 bit

Byte : 字节。数据存储的基本单位，比如移动硬盘 1T，单位是 byte

bit : 比特，又叫位。一个位要么是 0 要么是 1. 数据传输的单位 , 比如家里的宽带 100 MB，下载速度并没有达到 100MB，一般都是 12-13MB，那么是因为需要使用 100 / 8

关系：1Byte = 8bit

## 获取字符串 byte

```java
public class ByteBit {
    public static void main(String[] args) {
        String a = "a";
        byte[] bytes = a.getBytes();
        for (byte b : bytes) {
            int c = b;
            // 打印发现 byte 实际上就是 ascii 码
            System.out.println(c); // 97
        }
    }
}
```

## 获取字符串 bit

```java
public class ByteBit {
    public static void main(String[] args) {
        String a = "a";
        byte[] bytes = a.getBytes();
        for (byte b : bytes) {
            int c = b;
            // 打印发现 byte 实际上就是 ascii 码
            System.out.println(c); // 97
            // 我们在来看看每个 byte 对应的 bit，byte 获取对应的 bit
            String s = Integer.toBinaryString(c);
            System.out.println(s); // 1100001
        }
    }
}
```

## 中文对应的字节

```java
// 中文在 GBK 编码下，占据 2 个字节
// 中文在 UTF-8 编码下，占据 3 个字节
public class ByteBit {
    public static void main(String[] args) throws UnsupportedEncodingException {
        String a = "尚";
        HashMap<String, String> map = getByteBit(a);
        System.out.println(map.entrySet());
    }

    private static HashMap<String,String> getByteBit(String a) throws UnsupportedEncodingException {
        byte[] bytes = a.getBytes("UTF-8");
        HashMap<String, String> map = new HashMap<>();
        int i = 1;
        for (byte aByte : bytes) {
            String key = "第" + i + "个字节比特位";
            String value = String.valueOf("Byte 字节是：" + aByte + "" +"Bit 比特位是：" + Integer.toBinaryString(aByte));
            map.put(key, value);
            i++;
        }
        return map;
    }
}

//[第 1 个字节比特位 = Byte 字节是:-27 Bit 比特位是：11111111111111111111111111100101,
// 第 2 个字节比特位 = Byte 字节是:-80 Bit 比特位是：11111111111111111111111110110000,
// 第 3 个字节比特位 = Byte 字节是:-102 Bit 比特位是：11111111111111111111111110011010]
```

```java
// 我们修改 编码格式 , 编码格式改成 GBK，我们在运行发现变成了 2 个字节
public class ByteBit {
    public static void main(String[] args) throws UnsupportedEncodingException {
        String a = "尚";
        HashMap<String, String> map = getByteBit(a);
        System.out.println(map.entrySet());
    }

    private static HashMap<String,String> getByteBit(String a) throws UnsupportedEncodingException {
        byte[] bytes = a.getBytes("GBK");
        HashMap<String, String> map = new HashMap<>();
        int i = 1;
        for (byte aByte : bytes) {
            String key = "第" + i + "个字节比特位";
            String value = String.valueOf("Byte 字节是：" + aByte + "" +"Bit 比特位是：" + Integer.toBinaryString(aByte));
            map.put(key, value);
            i++;
        }
        return map;
    }
}

// [第 1 个字节比特位 = Byte 字节是:-55 Bit 比特位是：11111111111111111111111111001001,
// 第 2 个字节比特位 = Byte 字节是:-48 Bit 比特位是：11111111111111111111111111010000]
```

## 英文对应的字节

```java
// 英文对应的字节在不同的编码下都占用 1 个字节
public class ByteBit {
    public static void main(String[] args) throws UnsupportedEncodingException {
        String a = "A";
        HashMap<String, String> map = getByteBit(a);
        System.out.println(map.entrySet());
    }

    private static HashMap<String,String> getByteBit(String a) throws UnsupportedEncodingException {
        byte[] bytes = a.getBytes("UTF-8");
        HashMap<String, String> map = new HashMap<>();
        int i = 1;
        for (byte aByte : bytes) {
            String key = "第" + i + "个字节比特位";
            String value = String.valueOf("Byte 字节是：" + aByte + "" +"Bit 比特位是：" + Integer.toBinaryString(aByte));
            map.put(key, value);
            i++;
        }
        return map;
    }
}

// [第 1 个字节比特位 = Byte 字节是：65 Bit 比特位是：1000001]
```
